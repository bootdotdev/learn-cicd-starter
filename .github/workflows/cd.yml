name: CD

on:
  push:
    branches: [main]

jobs:
  Deploy:
    name: Deploy
    runs-on: ubuntu-latest
 
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.25.1'

      - name: Build production binary
        run: ./scripts/buildprod.sh
      
      - name: Authenticate to Google Cloud
        id: 'auth'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: Build and Push the Docker Image
        run: |
          gcloud builds submit \
            --tag us-central1-docker.pkg.dev/notely-472606/notely-ar-repo/notely:v1.0.1 . 
      
      - name: Deploy to Cloud Run
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/notely-472606/notely-ar-repo/notely:v1.0.1 --region us-central1 --allow-unauthenticated --project notely-472606 --max-instances=4
