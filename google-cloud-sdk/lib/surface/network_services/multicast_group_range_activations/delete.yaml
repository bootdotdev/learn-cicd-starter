release_tracks: [GA, BETA, ALPHA]
help_text:
  brief: |
    Delete a multicast group range activation.
  description: |
    Delete a multicast group range activation in the specified location of the current project.
  examples: |
    To delete a multicast group range activation in the current project, run:

      $ {command} my-mg-range-activation --location=zone
arguments:
  resource:
    spec: !REF googlecloudsdk.command_lib.network_services.resources:multicastGroupRangeActivation
    help_text: The multicast group range activation to delete.
async:
  collection: networkservices.projects.locations.operations
request: &request
  GA:
    api_version: v1
  ALPHA:
    api_version: v1alpha1
  BETA:
    api_version: v1beta1
  collection: networkservices.projects.locations.multicastGroupRangeActivations
